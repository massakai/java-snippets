# Repository Guidelines

このリポジトリは Java / Spring Boot の小さな検証コード置き場です。
AI エージェントや共同作業者が迷わないように、まずここを基準に動きます。

## 役割

- 各サブディレクトリを独立したサンプルプロジェクトとして扱う
- ブログ記事や技術メモで参照する検証済みコードを置く
- 1つのサンプルでは、確認したい挙動が読み取りやすい最小構成を優先する

## サブエージェント運用

- サブエージェントは常設担当として扱わず、必要時だけ起動する
- サンプル固有の前提は会話履歴ではなく、各サンプル配下の `README.md` や `AGENTS.md` に残す
- 単一サンプルの調査や修正では、そのサンプルだけを読む `sample-owner` 相当の役割で進める
- ブログ記事とコードの整合確認では、クラス名、プロパティ名、実行コマンド、Spring Boot バージョン、出力例の一致を優先して確認する
- ルート `README.md`、workflow、共通方針の整理は `repo-maintainer` 相当の横断タスクとして扱い、サンプル固有の文脈と分離する
- サブエージェントに渡す作業範囲は、原則として単一サンプルまたは明確な横断確認に限定する

## ドキュメント責務

- ルート `AGENTS.md` には、リポジトリ全体に共通する運用ルール、Git / Pull Request / GitHub Actions 方針、横断的な編集方針を書く
- ルート `README.md` には、各サンプルの一覧と概要、リポジトリ全体の入口として必要な情報を書く
- 各サンプル配下の `README.md` には、そのサンプルの目的、確認できること、実行方法、対象バージョンなど、利用者向けの説明を書く
- 各サンプル配下の `AGENTS.md` には、そのサンプル固有の前提、確認観点、記事との対応、命名上の注意など、作業者向けの補足を書く
- リポジトリ全体の共通ルールを各サンプル配下に重複して書かない
- サンプル固有の前提や確認ポイントは、会話履歴だけに残さず、必要に応じてサンプル配下の `README.md` または `AGENTS.md` に反映する

### 理由

- ルートとサンプル配下で責務を分けると、共通ルールとサンプル固有の文脈が混ざりにくい
- 横断ルールの修正箇所をルートに集約すると、更新漏れや重複記載を減らせる
- サンプル固有の前提を配下ドキュメントに残すことで、後から読む人や別の作業者が会話履歴なしでも判断しやすくなる

## 基本方針

- サンプルは目的が分かる英語の kebab-case ディレクトリに置く
- ルートの `README.md` にサンプル名と概要を追記する
- サンプル配下にも `README.md` を置き、目的、確認できること、実行方法を記載する
- 記事から参照されるコードは、本文とクラス名、プロパティ名、ファイル名がずれないようにする
- ブログ記事用の検証であっても、リポジトリ単体で意図が分かる説明を残す

## Spring Boot サンプル

- Gradleプロジェクトには Gradle wrapper を置き、原則として `gradle` ではなく `./gradlew` で実行する
- Gradle wrapper のバージョンは原則 9.5.1 に揃え、変更する場合はREADMEやworkflowに理由を残す
- 設定ファイルのYAML拡張子は `.yaml` に統一する
- `application.yml` や workflow の `.yml` は使わず、`.yaml` を使う
- Spring Boot のバージョン差を確認する場合は、GradleプロパティやREADMEに検証対象を明記する
- テストで挙動を示せる場合は、できるだけ自動テストとして残す

## コード構成

- Javaパッケージは原則 `com.github.massakai.snippets` 配下に置く
- サンプル名が分かる短いサブパッケージ名を使う
- 検証目的に直接関係しない依存関係や設定は追加しない
- 依存関係を追加した場合は、サンプルREADMEに目的を簡潔に記載する

## 検証

- 変更したサンプル配下で `./gradlew test` を実行して確認する
- Spring Boot バージョン差を扱うサンプルでは、READMEやworkflowに記載した対象バージョンで確認する
- Pull Request の本文や作業結果には、実行した確認コマンドを残す
- 複数の Java や Spring Boot の組み合わせを確認した場合は、確認した組み合わせを明記する
- 実行できなかった確認がある場合は、未確認範囲を明記する
- テストを実行できなかった場合は、理由と未確認範囲を作業結果に残す
- テスト以外の手動確認を行った場合も、確認内容と結果を作業結果に残す

## GitHub Actions

- サンプルを追加したら、必要に応じて `.github/workflows/*.yaml` にテスト実行を追加する
- workflow ファイル名も `.yaml` を使う
- CIで確認しているJava、Gradle、Spring BootのバージョンはREADMEまたは記事側で参照できるようにする
- CI対象にしないサンプルは、READMEに理由と手動確認手順を書く
- Pull Request の検証は原則として `pull_request` イベントで行う
- `push` イベントでの実行は原則として `master` への push のみに限定する
- `push` と `pull_request` を併用する workflow では、同一コミットに対する重複実行が起きないか確認する
- matrix を追加・変更する場合は、イベント数 × Java バージョン数 × Spring Boot バージョン数 などで実行数を見積もる
- workflow 追加や matrix 拡張を行う場合は、チェック数や実行時間が過剰になっていないか Pull Request 上で確認する

## Git ベースライン

- ブランチ戦略は GitHub Flow とし、`master` をデフォルトブランチとして扱う
- 作業開始前に `git fetch origin master` を実行し、`origin/master` が最新か確認する
- 作業開始前に `git status --branch` と `git show -s --format=... HEAD origin/master` などで、現在の作業位置と `origin/master` の差を確認する
- 必要なら `git branch -f master origin/master` で、ローカル `master` の参照を最新の `origin/master` に合わせてよい
- これはローカル `master` を作業用に更新するためではなく、最新のデフォルトブランチ位置を明確にするための参照追従として扱う
- 作業は最新の `origin/master` を基点に作業ブランチを作って始める
- detached HEAD や古いローカルブランチを基点にしたまま編集を始めない
- ローカル `master` はリモート取り込み専用とし、直接コミットしない
- `master` では作業せず、変更は必ず最新 `origin/master` を基点に切った作業ブランチで行う
- 一時退避のために `git stash` を使った場合は、作業終了時に `git stash list` を確認する
- 今回の作業に不要な stash を新たに残したまま完了扱いにしない
- デフォルトブランチへの直接 push は行わず、必ず Pull Request 経由で取り込む
- サンプルコード変更と、リポジトリ全体の workflow / 運用変更は、レビュー観点が異なるため原則として別ブランチ・別 Pull Request に分ける

### 理由

- 古い `master` や detached HEAD を基点にすると、最新 `master` 取り込み後に差分の見直しや載せ替えが必要になりやすい
- 作業開始前に基点を揃えることで、レビュー時の差分が素直になり、余計なコンフリクトや混線を減らせる
- `master` を取り込み専用にし、参照追従だけを許可すると、ローカル作業とデフォルトブランチの状態が分離され、意図しないコミットや push を防ぎやすい
- stash の残存を確認する運用にすると、一時退避した変更の置き忘れや、別作業への混入を防ぎやすい
- workflow や共通運用変更をサンプル修正と分けると、コードレビューで見る観点を切り分けやすい

## Pull Request の粒度

- サンプルの内容に関する修正は、原則として対象サンプルごとに Pull Request を分ける
- ここでいうサンプルの内容には、挙動、設定、依存関係、テスト、README、記事との整合を含む
- リポジトリ全体の共通方針に関する変更は、複数サンプルにまたがっていても、同じ意図の変更であれば 1 つの Pull Request にまとめてよい
- ここでいう共通方針には、コーディングスタイル、命名、共通ドキュメント、GitHub Actions / workflow の運用方針を含む
- ただし、サンプル固有の修正とリポジトリ全体の方針変更は、原則として同じ Pull Request に混在させない
- 共通方針の変更でも、レビュー観点が大きく異なる場合は別 Pull Request に分ける
- 共通変更をまとめる場合でも、Pull Request は小さく保ち、変更理由と適用方針を説明しやすい範囲に留める
- `final` の付与、import 整理、命名統一、フォーマット統一、workflow 方針変更のような横断的で同質な変更は、リポジトリ全体の Pull Request としてまとめてよい
- サンプル A の挙動修正とリポジトリ全体のスタイル変更、サンプル固有の不具合修正と CI / workflow 方針変更のように、レビュー観点の異なる変更は同じ Pull Request に混在させない
- 既存の Pull Request に追加コミットを積む場合は、何を追加・修正したコミットかがコミットメッセージと差分から分かる粒度で積む
- 既存の Pull Request にレビュー観点の異なる方針変更が混ざり始めたら、原則として別ブランチ・別 Pull Request に切り出す
- 実装変更と、その変更内容を検証するテストの追加・更新は、原則として同一コミットに含める
- 挙動変更だけ、またはテストだけが一時的に存在する中間コミットをなるべく作らない
- ただし、機械的リファクタやテスト基盤整備のように、レビュー観点を分けたほうが分かりやすい場合は別コミットでもよい

### 理由

- サンプル固有の変更と共通方針の変更では、レビューで確認したい観点が異なる
- 変更の意図ごとに Pull Request を分けることで、差分の意味が追いやすくなり、レビューや差し戻しがしやすくなる
- コーディングスタイルや workflow 方針のような横断的変更は、まとめて適用したほうが一貫性を保ちやすい
- 具体例を示しておくと、どの変更をまとめてよいか、どこで Pull Request を分けるべきか判断しやすい
- 既存の Pull Request に追加コミットを積むときも粒度を保つことで、途中経過と最終差分の両方を追いやすくなる
- 実装とテストを同じコミットに含めると、どの変更で何を確認できるようになったかを履歴から追いやすい

## Pull Request

- Pull Request のタイトルと本文は原則として日本語で書く
- Pull Request のタイトルに `[codex]` は付けない
- Codex で作成した Pull Request には `codex` ラベルを付ける
- Pull Request は原則 Draft で作成し、レビュー可能になった段階で Ready for review にする
- Pull Request の本文には、少なくとも変更概要、背景、確認内容を書く

### gh の運用

- `gh pr create` や `gh pr edit` で本文を渡すときは、原則として `--body-file` を使う
- バッククォートや角括弧など、shell 展開の影響を受けやすい記号を含む本文をインライン文字列で渡さない
- `gh pr create --draft` 実行後は、`gh pr view --json isDraft,body` などで Draft 状態と本文を確認する
- 既存 Pull Request の本文を `gh` で更新した場合も、意図どおり反映されたか確認する

### 理由

- Pull Request 本文は Markdown 記法を含みやすく、shell 経由でインライン指定すると意図せず展開されることがある
- Pull Request の本文や Draft 状態は、コマンド指定どおりに見えても結果がずれることがあるため、作成・更新後の確認が必要

## 編集時の注意

- 既存サンプルの目的と無関係な整形や大規模な並べ替えは避ける
- 生成物、IDE設定、OS由来のファイルはコミットしない
- 既存の未追跡ファイルや変更は、依頼内容と無関係なら触らない
- サンプルコードは説明のための簡潔さを優先しつつ、テストで実際に動く状態を保つ
